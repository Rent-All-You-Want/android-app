package com.pablojuice.rayw.feature.profile.data

import org.junit.Assert
import org.junit.Test

class ProfileRepositoryTest {
    @Test
    fun addition_isCorrect() {
        Assert.assertEquals(4, 2 + 2)
    }
}